{
  "devDependencies": {
    "@bokub/prettier-config": "^2.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.4",
    "prettier": "^3.6.2"
  },
  "prettier": "@bokub/prettier-config",
  "scripts": {
    "prepare": "husky",
    "zip": "rm -f conso-downloader.zip && zip -r conso-downloader.zip images/* *.js manifest.json"
  },
  "lint-staged": {
    "*": "prettier --write --ignore-unknown"
  }
}
